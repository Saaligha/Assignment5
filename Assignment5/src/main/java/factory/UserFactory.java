package factory;
import domain.Nationality;
import util.Misc;

public class UserFactory {

    public static UserFactory{

        public static Nationality(String nationality){
            return new Nationality.Builder().nationality(Misc.generateId())
                    .nationality(id);
            .build();
        }
    }

}
